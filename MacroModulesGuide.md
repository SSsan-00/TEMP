# VBAマクロ4モジュールの処理内容・利用手順

対象:
- `BetaEvidenceGenerator.bas`
- `BetaTestCaseGenerator.bas`
- `ConditionalBranchChecker.bas`
- `EscapePartsMarking.bas`

## 共通の使い方

1. VBAエディタで各 `.bas` を標準モジュールとしてインポートします。
2. 実行時は `RunMain` をモジュール名つきで呼び出します。  
   例: `Call BetaEvidenceGenerator.RunMain`
3. 処理対象ブックへ直接書き込むマクロがあるため、実行前にバックアップを推奨します。

---

## 1. `BetaEvidenceGenerator.bas`

### 処理内容（概要）
- 参照元 `.xlsx` を読み取り、**新しい出力 `.xlsx`** を作成します（共通/個別を別ファイルで出力）。
- `REFER` シートをキーに、`referValue` と出力ファイル名構成要素（α/β/γ）を取得します。
- 参照元シートの候補:
  - 共通: `【共通】` + `referValue`
  - 個別: `【個別】` + `referValue`
- 共通出力では `A1-1-1` を先頭シートとして作成し、`A3`/`B3` の `〇〇〇` のみ `baseName` に置換します。
- 参照元の `A/B/C` 列を行8から走査し、`A` をシート切替、`B/C` を30行刻みスロットへ転記します。
- 同名出力ファイルがあれば `_001`, `_002` の連番で別名保存します。

### 前提（テンプレ・列）
- マクロブック（`ThisWorkbook`）に以下が必要:
  - `A1-1-1`, `A1`, `REFER`
- `REFER` 列:
  - キー: `E`（入力ファイル名と完全一致）
  - `referValue`: `F`
  - β: `D`（2桁化）
  - α: `J`（拡張子除去）

### 利用手順
1. `BetaEvidenceGenerator.RunMain` を実行。
2. ダイアログで参照元 `.xlsx` を選択。
3. 入力ファイル名（例: `menu/mainmenu.php`）を入力。
4. 完了メッセージで共通/個別の出力先を確認。

### 出力
- 参照元と同じフォルダに以下を生成:
  - `<α>_【共通】<β><γ>_単体テストエビデンス_初期開発.xlsx`
  - `<α>_【個別】<β><γ>_単体テストエビデンス_初期開発.xlsx`

---

## 2. `BetaTestCaseGenerator.bas`

### 処理内容（概要）
- 入力した機能連番をもとに `REFER` を検索し、テストケース用 `.xlsx` を新規作成します。
- `REFER` の `J` 列で**部分一致**ヒットした全行を対象に、α/β/γを収集します。
- 生成シート:
  - 各ヒットごと: `【共通】<β>`, `【個別】<β>`, `現行ソース（PHP）<β>`
  - 1枚のみ: `⇒参考`, `現行画面`
- セル設定:
  - `【共通】<β>` / `【個別】<β>`: `BD1=β`, `BD13=機能連番`
  - `現行ソース（PHP）<β>`: `C4=γ`
- 同名ファイルがあれば `_001` 連番で保存します。
- 新規ブック初期シート（`Sheet1` など）は最後に削除します。

### 前提（テンプレ・列）
- マクロブックに以下テンプレが必要:
  - `【共通】機能名`, `【個別】機能名`, `⇒参考`, `現行ソース（PHP）`, `現行画面`
- `REFER` 列:
  - α: `J`（拡張子除去）
  - β: `F`
  - γ: `E`

### 利用手順
1. `BetaTestCaseGenerator.RunMain` を実行。
2. 機能連番（例: `S99-999-99`）を入力。
3. 保存先を確認（`ThisWorkbook` 保存済みなら同フォルダ、未保存ならSaveAsダイアログ）。
4. 出力ファイルを確認。

### 出力
- `<α>_単体テストケース_初期開発.xlsx`（必要に応じて連番付き）

---

## 3. `ConditionalBranchChecker.bas`

### 処理内容（概要）
- 対象 `.xlsx` の「現行ソース」シートを解析して、B列マーキングと個別シート追記を行います。
- 現行ソースシート判定:
  - シート名に `現行ソース` を含む候補を収集
  - 複数候補時は、抽出した機能名が入力機能名と**完全一致**するシートのみ採用
- マーキング:
  - 解析対象はC列
  - `#` / `//` のコメント行は除外
  - `<!doctype`, `<html`, `<head` のいずれかを検出したら以降は検索停止
  - `function` 行は `Bn`、その他対象構文は `Bn-` をB列へ出力
- 個別シート（`【個別】<機能名>`）があれば、検出イベントを所定フォーマットで追記します。
- 行追加ロジック:
  - 開始時に個別シート `15～24行`（10行）を退避
  - テンプレート領域を超えそうなとき、`α-10` に100行まとめて挿入
  - 挿入行は退避した10行を繰り返し使用

### 利用手順
1. `ConditionalBranchChecker.RunMain` を実行。
2. 機能名を入力。
3. 対象 `.xlsx` を選択。
4. 処理完了後、対象ブック（現行ソースB列・個別シート）を確認。

### 注意
- このマクロは対象ブックを**直接更新して保存**します（別名出力ではありません）。

---

## 4. `EscapePartsMarking.bas`

### 処理内容（概要）
- 選択したExcelの `A1-1-1` シートだけを対象に、B列文字列中の `prefix(...)` を赤太字化します（B4以降）。
- `prefix` はモジュール内 `LoadPrefixesFromCode` の配列から読み込みます（初期値: `sqlS`, `sqlN`）。
- 実行時にC列へ記載するメッセージを入力できます（空欄時は既定値 `SQLインジェクション対策完了`）。
- ヒットした行はC列に上記メッセージを赤字で設定します。
- 対象ブックを保存して閉じます。

### 前提
- 対象ブックに `A1-1-1` シートが必要。
- エスケープ関数を追加する場合は `EscapePartsMarking.bas` の `LoadPrefixesFromCode` 内 `Array(...)` に追記します。

### 利用手順
1. `EscapePartsMarking.RunMain` を実行。
2. 加工対象ファイル（`.xlsx/.xls/.xlsm/.xlsb`）を選択。
3. C列へ記載するメッセージを入力（空欄なら既定値）。
4. 完了後、`A1-1-1` シート B/C列の反映結果を確認。

### 注意
- `prefix(` の後ろで最初に現れる `)` までを装飾対象にします（厳密な括弧ネスト解析ではありません）。
